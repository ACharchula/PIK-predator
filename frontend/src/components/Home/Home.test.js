import React from 'react';
import Home from './Home';
import { mount } from 'enzyme';


describe('Home', () => {
    let wrapper;

    /*beforeEach(() => {
        wrapper = mount(
            <Home/>
        );
    });*/

    test('callingProducts', () => {
        /*expect(
            getProducts

        ).toHaveBeenCalled();*/
        expect(true).toBe(true);
    })

    /*afterEach(() => {
        wrapper = umount(
            <Home/>
        );
    });*/

})
